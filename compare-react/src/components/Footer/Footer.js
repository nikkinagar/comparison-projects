import React from 'react';

/*Stateless component - when you know the component 
does not need functionality or access to state 
(used for static info) */

const Footer = () => {
    return (
      <div className="App">
        my footer
      </div>
    );
  };

export default Footer;
